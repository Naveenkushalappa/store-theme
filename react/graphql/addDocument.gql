mutation addDocument($acronym: String!, $document: DocumentInput!) @context(provider: "vtex.store-graphql@2.x") {
    createDocument(acronym: $acronym, document: $document){
        id
        documentId
    }
}